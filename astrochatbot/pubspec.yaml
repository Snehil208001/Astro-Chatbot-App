name: astrochatbot
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  # CHANGED: Fixed this to range 3.0.0 to 4.0.0 (Valid Dart version)
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  flutter_dotenv: ^5.1.0
  # CHANGED: Downgraded to a version compatible with your Flutter setup
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env